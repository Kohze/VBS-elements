import {
  Meta,
  Canvas,
  ArgsTable,
  Story,
  Preview,
  Source,
} from '@storybook/addon-docs'
import Breadcrumb from '@/components/application-ui/navigation/breadcrumb'
import heroIconsNames from '@/assets/icons/hero-icons/names'

<Meta
  title="Application UI/Navigation/Breadcrumb"
  component={Breadcrumb}
  argTypes={{
    homeIconName: {
      control: {
        type: 'select',
        options: heroIconsNames,
      },
    },
    dividerIconName: {
      control: {
        type: 'select',
        options: heroIconsNames,
      },
    },
  }}
  parameters={{
    docs: {
      source: {
        type: 'code',
      },
    },
  }}
/>

export const Simple = (args) => (
  <Breadcrumb
    pages={[
      { name: 'Projects', href: '#', current: false },
      { name: 'Project Nero', href: '#', current: false },
      { name: 'Settings', href: '#', current: true },
    ]}
    {...args}
  />
)

export const SimpleWithCustomIcons = (args) => (
  <Breadcrumb
    homeIconName="home-modern"
    dividerIconName="minus-small"
    pages={[
      { name: 'Projects', href: '#', current: false },
      { name: 'Project Nero', href: '#', current: false },
      { name: 'Settings', href: '#', current: true },
    ]}
    {...args}
  />
)

export const BarCollapsed = (args) => (
  <Breadcrumb
    variant="bar"
    pages={[
      { name: 'Projects', href: '#', current: false },
      { name: 'Project Nero', href: '#', current: false },
      { name: 'Settings', href: '#', current: true },
    ]}
    {...args}
  />
)

export const BarCollapsedFullWidth = (args) => (
  <Breadcrumb
    variant="bar"
    fullWidth
    pages={[
      { name: 'Projects', href: '#', current: false },
      { name: 'Project Nero', href: '#', current: false },
      { name: 'Settings', href: '#', current: true },
    ]}
    {...args}
  />
)

# Breadcrumb

Breadcrumb is a navigation component that shows the current page’s location within a navigational hierarchy.

Breadcrumbs needs to have a list of `pages` to display. Each page is an object with the following properties: `name`, `href` and `current`. The `current` property is used to indicate the current page.

## Import

```js
import Breadcrumb from '@/components/application-ui/navigation/breadcrumb'
```

## Simple

<Canvas>
  <Story name="Simple">{Simple.bind({})}</Story>
</Canvas>

## Simple with custom icons

Home icon and divider icon can be customized by passing the `homeIconName` and `dividerIconName` props. Check the referance for all props.

<Canvas>
  <Story name="Simple with custom icons">
    {SimpleWithCustomIcons.bind({})}
  </Story>
</Canvas>

## Bar Collapsed

Collapsed bar variant is used when the breadcrumb is displayed in a small space. Use the `variant` prop to set the variant to `bar`.

<Canvas>
  <Story name="Bar Collapsed">{BarCollapsed.bind({})}</Story>
</Canvas>

## Bar Collapsed full width

Collapsed bar variant can be set to full width by passing the `fullWidth` prop.

<Canvas>
  <Story name="Bar Collapsed full width">
    {BarCollapsedFullWidth.bind({})}
  </Story>
</Canvas>

## Reference

<ArgsTable of={Breadcrumb} />
