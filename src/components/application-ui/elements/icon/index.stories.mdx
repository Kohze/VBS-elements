import {
  Meta,
  Canvas,
  ArgsTable,
  Story,
  Preview,
  Source,
} from '@storybook/addon-docs'
import VBSIcon from '@/components/application-ui/elements/icon'
import heroIconsNames from '@/assets/icons/hero-icons/names'

<Meta
  title="Application UI/Elements/Icon"
  component={VBSIcon}
  argTypes={{
    iconName: {
      control: {
        type: 'select',
        options: heroIconsNames,
      },
    },
  }}
/>

export const Template = (args) => <VBSIcon {...args} iconName="bug-ant" />

export const Sizes = (args) => (
  <div className="flex flex-col space-y-4">
    <VBSIcon {...args} iconName="bug-ant" size="xs" />
    <VBSIcon {...args} iconName="bug-ant" size="sm" />
    <VBSIcon {...args} iconName="bug-ant" size="md" />
    <VBSIcon {...args} iconName="bug-ant" size="lg" />
    <VBSIcon {...args} iconName="bug-ant" size="xl" />
  </div>
)

# Icon

Icon component for displaying icons. The icon component is a wrapper for the [Hero Icons](https://heroicons.com/) library.

## Import

```js
import VBSIcon from '@/components/application-ui/elements/icon'
```

## Usage

To use the icon component, simply pass the `iconName` as a prop. The `iconName` prop is the name of the icon you want to display. The icon name is the same as the icon name in the [Hero Icons](https://heroicons.com/) library.

## Sample

<Canvas>
  <Story name="Sample">{Template.bind({})}</Story>
</Canvas>

## Outline

The icon component supports outline icons. To use an outline icon, pass the `iconType` prop with the value `outline`.

<Canvas>
  <Story
    name="Outline"
    args={{
      iconType: 'outline',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Sizes

The icon component supports different sizes. To use a different size, pass the `size` prop with the value `xs`, `sm`, `md`, `lg`, or `xl`.

<Canvas>
  <Story name="Sizes">{Sizes.bind({})}</Story>
</Canvas>

## Reference

<ArgsTable of={VBSIcon} />
