import {
  Meta,
  Canvas,
  ArgsTable,
  Story,
  Preview,
  Source,
} from '@storybook/addon-docs'
import VBSBadge from '@/components/application-ui/elements/badge'

<Meta title="Application UI/Elements/Badge" component={VBSBadge} />

export const Template = (args) => <VBSBadge text="Badge" {...args} />

export const Variants = (args) => (
  <div className="flex gap-2 items-center">
    <VBSBadge text="Primary" {...args} />
    <VBSBadge text="Secondary" variant="secondary" {...args} />
    <VBSBadge text="Success" variant="success" {...args} />
    <VBSBadge text="Danger" variant="danger" {...args} />
    <VBSBadge text="Warning" variant="warning" {...args} />
    <VBSBadge text="Info" variant="info" {...args} />
    <VBSBadge text="Outline" variant="outline" {...args} />
    <VBSBadge text="Light" variant="light" {...args} />
    <VBSBadge text="Dark" variant="dark" {...args} />
  </div>
)

export const Kinds = (args) => (
  <div className="flex gap-2 items-center">
    <VBSBadge text="Square" kind="square" {...args} />
    <VBSBadge text="Rounded" kind="rounded" {...args} />
    <VBSBadge text="Circular" kind="circular" {...args} />
  </div>
)

export const Sizes = (args) => (
  <div className="flex gap-2 items-end">
    <VBSBadge text="Small" size="sm" {...args} />
    <VBSBadge text="Normal" {...args} />
  </div>
)

export const WithNotification = (args) => (
  <div className="flex gap-2 items-end">
    <VBSBadge text="Badge" notification size="sm" {...args} />
    <VBSBadge text="Badge" notification {...args} />
  </div>
)

export const WithRemoveButton = (args) => (
  <div className="flex gap-2 items-end">
    <VBSBadge text="Badge" size="sm" withRemove {...args} />
    <VBSBadge text="Badge" withRemove {...args} />
  </div>
)

# Badge

Badges are used to highlight important information.

## Import

```js
import VBSBadge from '@/components/application-ui/elements/badge'
```

## Default

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

## Variants

Variants are used to highlight the importance of the badge. The default variant is `primary`. The other variants are `secondary`, `success`, `danger`, `warning`, `info`, `outline`, `light`, and `dark`.

<Canvas>
  <Story name="Variants">{Variants.bind({})}</Story>
</Canvas>

## Kinds

Kinds are used to change the shape of the badge. The default kind is `rounded`. The other kinds are `square` and `circular`.

<Canvas>
  <Story name="Kinds">{Kinds.bind({})}</Story>
</Canvas>

## Sizes

Sizes are used to change the size of the badge. The default size is `normal`. The other size is `small`.

<Canvas>
  <Story name="Sizes">{Sizes.bind({})}</Story>
</Canvas>

## With Notification

Badges can have a notification dot with `notification` prop.

<Canvas>
  <Story name="With Notification">{WithNotification.bind({})}</Story>
</Canvas>

## With Remove Button

Badges can have a remove button with a callback function with `removeButton` and `onRemove` props.

<Canvas>
  <Story name="With Remove Button">{WithRemoveButton.bind({})}</Story>
</Canvas>

## Reference

<ArgsTable of={VBSBadge} />
