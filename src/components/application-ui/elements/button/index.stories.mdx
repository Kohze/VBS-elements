import {
  Meta,
  Canvas,
  ArgsTable,
  Story,
  Preview,
  Source,
} from '@storybook/addon-docs'
import VBSButton from '@/components/application-ui/elements/button'
import heroIconsNames from '@/assets/icons/hero-icons/names'

<Meta
  title="Application UI/Elements/Button"
  component={VBSButton}
  argTypes={{
    backgroundColor: { control: 'color' },
    color: { control: 'color' },
    iconName: {
      control: {
        type: 'select',
        options: heroIconsNames,
      },
    },
    onClick: { action: 'clicked' },
  }}
  parameters={{
    colorPicker: {
      applyColorTo: ['backgroundColor', 'color'],
    },
  }}
/>

export const Template = (args) => <VBSButton {...args} />
export const Default = Template.bind({})
Default.args = {
  text: 'Default Button',
}

export const Variants = (args) => (
  <div class="flex-row-gaps">
    <VBSButton {...args} text="Primary" variant="primary" />
    <VBSButton {...args} text="Secondary" variant="secondary" />
    <VBSButton {...args} text="Tertiary" variant="tertiary" />
    <VBSButton {...args} text="Danger" variant="danger" />
    <VBSButton {...args} text="Success" variant="success" />
    <VBSButton {...args} text="Warning" variant="warning" />
    <VBSButton {...args} text="Info" variant="info" />
    <VBSButton {...args} text="Outline" variant="outline" />
    <VBSButton {...args} text="Light" variant="light" />
    <VBSButton {...args} text="Dark" variant="dark" />
  </div>
)

export const Sizes = (args) => (
  <div className="flex-row-gaps-items-end">
    <VBSButton {...args} text="XSmall" size="xs" />
    <VBSButton {...args} text="Small" size="sm" />
    <VBSButton {...args} text="Medium" size="md" />
    <VBSButton {...args} text="Large" size="lg" />
    <VBSButton {...args} text="XLarge" size="xl" />
  </div>
)

export const Disabled = (args) => (
  <VBSButton {...args} text="Disabled" disabled />
)

export const FullWidth = (args) => (
  <VBSButton {...args} text="Full Width" fullWidth />
)

export const WithIcon = (args) => <VBSButton {...args} iconName="cloud" />

export const WithIconAndText = (args) => (
  <div className="flex-row-gaps">
    <VBSButton {...args} text="With Icon and Text" iconName="cloud" />
    <VBSButton
      {...args}
      text="With Icon and Text"
      iconName="cloud"
      iconPosition="right"
    />
  </div>
)

export const FuncButton = (args) => (
  <VBSButton {...args} text="Click Me" onClick={() => alert('clicked !')} />
)

# Button

Buttons allow users to take actions, and make choices with a single tap.

## Import

```js
import VBSIcon from '@/components/application-ui/elements/icon'
```

## Usage

- A button should contain at least one text, icon or both.
- Button texts are limited to one line.
- Buttons can be used to trigger an action, or to navigate to a new page with href attribute.
- For button colors, `backgroundColor` and `color` props or tailwind classes can be used.
- For button sizes, `size` prop or tailwind classes can be used.
- All button styles can be customized with tailwind classes.

## Default

<Canvas>
  <Story name="Default">{Default}</Story>
</Canvas>

## Variants

There are two variants: `default` and `outline`. You can change the variant with `variant` prop.

<Canvas>
  <Story name="Variants">{Variants.bind({})}</Story>
</Canvas>

## Sizes

There are five sizes: `xs`, `sm`, `md`, `lg` and `xl`. You can change the size with `size` prop.

<Canvas>
  <Story name="Sizes">{Sizes.bind({})}</Story>
</Canvas>

## Disabled

You can disable a button with `disabled` prop.

<Canvas>
  <Story name="Disabled">{Disabled.bind({})}</Story>
</Canvas>

## Full Width

You can make a button full width with `fullWidth` prop.

<Canvas>
  <Story name="Full Width">{FullWidth.bind({})}</Story>
</Canvas>

## With Icon

You can add an icon to a button with `iconName` prop.

<Canvas>
  <Story name="With Icon">{WithIcon.bind({})}</Story>
</Canvas>

## With Icon and Text

You can add an icon to a button with `iconName` prop. You can change the icon position with `iconPosition` prop.

<Canvas>
  <Story name="With Icon and Text">{WithIconAndText.bind({})}</Story>
</Canvas>

## Function Button

You can add a function to a button with `@click` event.

<Canvas>
  <Story name="Function Button">{FuncButton.bind({})}</Story>
</Canvas>

## Reference

<ArgsTable of={VBSButton} />
