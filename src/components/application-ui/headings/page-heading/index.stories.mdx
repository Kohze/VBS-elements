import {
  Meta,
  Canvas,
  ArgsTable,
  Story,
  Preview,
  Source,
} from '@storybook/addon-docs'
import VBSPageHeading from '@/components/application-ui/headings/page-heading'
import VBSButton from '@/components/application-ui/elements/button'

<Meta
  title="Application UI/Headings/Page Heading"
  component={VBSPageHeading}
  parameters={{
    docs: {
      source: {
        type: 'code',
      },
    },
  }}
/>

export const Template = (args) => <VBSPageHeading {...args} />

export const Simple = (args) => (
  <VBSPageHeading
    title="Frontend Developer"
    actionButtons={(props) => (
      <>
        <VBSButton text="Edit" size="md" variant="outline" />
        <VBSButton text="Publish" size="md" variant="dark" />
      </>
    )}
    {...args}
  />
)

export const SimpleDark = (args) => (
  <VBSPageHeading
    title="Frontend Developer"
    theme="dark"
    actionButtons={(props) => (
      <>
        <VBSButton text="Edit" size="md" variant="outline" />
        <VBSButton text="Publish" size="md" variant="dark" />
      </>
    )}
    {...args}
  />
)

export const WithBreadcrumbs = (args) => (
  <VBSPageHeading
    title="Frontend Developer"
    breadcrumbs={[
      { name: 'Jobs', href: '/jobs', current: false },
      {
        name: 'Frontend Developer',
        href: '/jobs/frontend-developer',
        current: true,
      },
    ]}
    actionButtons={(props) => (
      <>
        <VBSButton text="Edit" size="md" variant="outline" />
        <VBSButton text="Publish" size="md" variant="dark" />
      </>
    )}
    {...args}
  />
)

export const WithMetaList = (args) => (
  <VBSPageHeading
    title="Frontend Developer"
    metas={[
      { name: 'Full-time', iconName: 'briefcase' },
      { name: 'Remote', iconName: 'map-pin' },
      { name: 'Part-time', iconName: 'clock' },
    ]}
    actionButtons={(props) => (
      <>
        <VBSButton text="Edit" size="md" variant="outline" />
        <VBSButton text="Publish" size="md" variant="dark" />
      </>
    )}
    {...args}
  />
)

export const WithBreadcrumbsAndMetaList = (args) => (
  <VBSPageHeading
    title="Frontend Developer"
    breadcrumbs={[
      { name: 'Jobs', href: '/jobs', current: false },
      {
        name: 'Frontend Developer',
        href: '/jobs/frontend-developer',
        current: true,
      },
    ]}
    metas={[
      { name: 'Full-time', iconName: 'briefcase' },
      { name: 'Remote', iconName: 'map-pin' },
      { name: 'Part-time', iconName: 'clock' },
    ]}
    actionButtons={(props) => (
      <>
        <VBSButton text="Edit" size="md" variant="outline" />
        <VBSButton text="Publish" size="md" variant="dark" />
      </>
    )}
    {...args}
  />
)

# Page Heading

The page heading is used to display the title of a page.

Some parts of the page heading are optional, such as the action buttons. To add some part of as node, you can use the `render props` pattern.

## Import

```js
import VBSPageHeading from '@/components/application-ui/headings/page-heading'
```

## Simple

<Canvas>
  <Story name="Simple">{Simple.bind({})}</Story>
</Canvas>

## Simple Dark

<Canvas>
  <Story name="Simple Dark">{SimpleDark.bind({})}</Story>
</Canvas>

## With Breadcrumbs

<Canvas>
  <Story name="With Breadcrumbs">{WithBreadcrumbs.bind({})}</Story>
</Canvas>

## With Meta List

<Canvas>
  <Story name="With Meta List">{WithMetaList.bind({})}</Story>
</Canvas>

## With Breadcrumbs and Meta List

<Canvas>
  <Story name="With Breadcrumbs and Meta List">
    {WithBreadcrumbsAndMetaList.bind({})}
  </Story>
</Canvas>

## Reference

<ArgsTable of={VBSPageHeading} />
