import {
  Meta,
  Canvas,
  ArgsTable,
  Story,
  Preview,
  Source,
} from '@storybook/addon-docs'
import { SectionHeading } from '@/components/application-ui/headings'
import { Button, Dropdown } from '@/components/application-ui/elements'
import { Tab } from '@/components/application-ui/navigation'

<Meta
  title="Application UI/Headings/Section Heading"
  component={SectionHeading}
/>

export const Template = (args) => <SectionHeading {...args} />

# Section Heading

The section heading is used to separate content into sections.

## Import

```js
import { SectionHeading } from '@/components/application-ui/headings'
```

## Simple

Simple section heading with a title.

<Canvas>
  <Story
    name="Simple"
    args={{
      title: 'Section Heading',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Description

Section heading with a title and a description.

<Canvas>
  <Story
    name="With Description"
    args={{
      title: 'Section Heading',
      description: 'Checkout and Payments Team',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Action Buttons

Section heading with a title and action buttons. To add action buttons, pass a render function to the `actionButtons` prop.

<Canvas>
  <Story
    name="With Action Buttons"
    args={{
      title: 'Section Heading',
      actionButtons: (props) => (
        <>
          <Button text="Create" size="md" variant="outline" />
          <Button text="Share" size="md" className="bg-indigo-600" />
        </>
      ),
    }}
    parameters={{
      docs: {
        source: {
          code: `
<SectionHeading
  title="Section Heading"
  actionButtons={() => (
    <>
      <Button text="Create" size="md" variant="outline" />
      <Button text="Share" size="md" className="bg-indigo-600" />
    </>
  )}
/>
`,
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Label

Section heading with a title and a label.

<Canvas>
  <Story
    name="With Label"
    args={{
      title: 'Section Heading',
      label: 'in Engineering',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Tabs

Section heading with a title and tabs. To add tabs, pass a render function to the `renderTabs` prop. For more information on tabs, see the [Tabs](/docs/application-ui-navigation-tab--default-story) component.

<Canvas>
  <Story
    name="With Tabs"
    args={{
      title: 'Section Heading',
      renderTabs: () => (
        <Tab
          tabs={[
            { name: 'My Account', href: '#', current: true },
            { name: 'Company', href: '#', current: false },
            { name: 'Team Members', href: '#', current: false },
            { name: 'Billing', href: '#', current: false },
          ]}
        />
      ),
    }}
    parameters={{
      docs: {
        source: {
          code: `
<SectionHeading
  title="Section Heading"
  renderTabs={() => (
    <Tab
      tabs={[
        { name: 'My Account', href: '#', current: true },
        { name: 'Company', href: '#', current: false },
        { name: 'Team Members', href: '#', current: false },
        { name: 'Billing', href: '#', current: false },
      ]}
    />
  )}
/>
`,
        },
      },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Dropdown

Section heading with a title and a dropdown. To add a dropdown, pass a render function to the `renderDropdown` prop. For more information on dropdowns, see the [Dropdown](/docs/application-ui-elements-dropdown--minimal) component.

<Canvas>
  <Story
    name="With Dropdown"
    parameters={{
      docs: {
        source: {
          code: `
<SectionHeading
  title="Section Heading"
  renderDropdown={() => (
    <Dropdown
      minimal
      elements={[
        { text: 'Link 1' },
        { text: 'Link 2' },
        { text: 'Link 3' },
      ]}
    />
  )}
/>
`,
        },
      },
    }}
  >
    <div className="h-56">
      <SectionHeading
        title="Section Heading"
        renderDropdown={() => (
          <Dropdown
            minimal
            elements={[
              { text: 'Link 1' },
              { text: 'Link 2' },
              { text: 'Link 3' },
            ]}
          />
        )}
      />
    </div>
  </Story>
</Canvas>

## Reference

<ArgsTable of={SectionHeading} />
