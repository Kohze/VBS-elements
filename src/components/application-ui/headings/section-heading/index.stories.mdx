import {
  Meta,
  Canvas,
  ArgsTable,
  Story,
  Preview,
  Source,
} from '@storybook/addon-docs'
import SectionHeading from '@/components/application-ui/headings/section-heading'
import Button from '@/components/application-ui/elements/button'
import Tab from '@/components/application-ui/navigation/tab'
import Dropdown from '@/components/application-ui/elements/dropdown'

<Meta
  title="Application UI/Headings/Section Heading"
  component={SectionHeading}
  parameters={{
    docs: {
      source: {
        type: 'code',
      },
    },
  }}
/>

export const Simple = (args) => (
  <SectionHeading title="Section Heading" {...args} />
)

export const WithDescription = (args) => (
  <SectionHeading
    title="Section Heading"
    description="Checkout and Payments Team"
    {...args}
  />
)

export const WithActionButtons = (args) => (
  <SectionHeading
    title="Heading"
    actionButtons={(props) => (
      <>
        <Button text="Create" size="md" variant="outline" />
        <Button text="Share" size="md" className="bg-indigo-600" />
      </>
    )}
    {...args}
  />
)

export const WithLabel = (args) => (
  <SectionHeading title="Section Heading" label="in Engineering" {...args} />
)

export const WithTabs = () => (
  <SectionHeading
    title="Section Heading"
    renderTabs={() => (
      <Tab
        tabs={[
          { name: 'My Account', href: '#', current: true },
          { name: 'Company', href: '#', current: false },
          { name: 'Team Members', href: '#', current: false },
          { name: 'Billing', href: '#', current: false },
        ]}
      />
    )}
  />
)

export const WithDropdown = (args) => (
  <div className="h-56">
    <SectionHeading
      title="Section Heading"
      renderDropdown={() => (
        <Dropdown
          minimal
          elements={[
            { text: 'Link 1' },
            { text: 'Link 2' },
            { text: 'Link 3' },
          ]}
        />
      )}
      {...args}
    />
  </div>
)

## Section Heading

The section heading is used to separate content into sections.

## Simple

Simple section heading with a title.

<Canvas>
  <Story name="Simple">{Simple.bind({})}</Story>
</Canvas>

## With Description

Section heading with a title and a description.

<Canvas>
  <Story name="With Description">{WithDescription.bind({})}</Story>
</Canvas>

## With Action Buttons

Section heading with a title and action buttons. To add action buttons, pass a render function to the `actionButtons` prop.

<Canvas>
  <Story name="With Action Buttons">{WithActionButtons.bind({})}</Story>
</Canvas>

## With Label

Section heading with a title and a label.

<Canvas>
  <Story name="With Label">{WithLabel.bind({})}</Story>
</Canvas>

## With Tabs

Section heading with a title and tabs. To add tabs, pass a render function to the `renderTabs` prop. For more information on tabs, see the [Tabs](/docs/application-ui-navigation-tab--default-story) component.

<Canvas>
  <Story name="With Tabs">{WithTabs.bind({})}</Story>
</Canvas>

## With Dropdown

Section heading with a title and a dropdown. To add a dropdown, pass a render function to the `renderDropdown` prop. For more information on dropdowns, see the [Dropdown](/docs/application-ui-elements-dropdown--minimal) component.

<Canvas>
  <Story name="With Dropdown">{WithDropdown.bind({})}</Story>
</Canvas>

## Reference

<ArgsTable of={SectionHeading} />
