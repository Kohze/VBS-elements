import { Meta, Canvas, ArgsTable, Story } from '@storybook/addon-docs'
import { Divider } from '@/components/application-ui/layout'
import heroIconsNames from '@/assets/icons/hero-icons/names'

<Meta
  title="Application UI/Layout/Divider"
  component={Divider}
  argTypes={{
    iconName: {
      control: {
        type: 'select',
        options: heroIconsNames,
      },
    },
    buttonIconName: {
      control: {
        type: 'select',
        options: heroIconsNames,
      },
    },
  }}
/>

export const Template = (args) => (
  <div className="py-10">
    <Divider {...args} />
  </div>
)

export const Positions = (args) => (
  <div>
    <div className="py-10">
      <Divider position="left" variant="with-icon" iconName="plus" {...args} />
    </div>
    <div className="py-10">
      <Divider variant="with-icon" iconName="plus" {...args} />
    </div>
    <div className="py-10">
      <Divider position="right" variant="with-icon" iconName="plus" {...args} />
    </div>
  </div>
)

# Divider

The divider component is used to separate content.

## Import

```js
import { Divider } from '@/components/application-ui/layout'
```

## Default

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

## With Label

Use the `variant` prop as "with-label" and the `label` prop to set the label text.

<Canvas>
  <Story
    name="With Label"
    args={{
      variant: 'with-label',
      label: 'Label',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Icon

Use the `variant` prop as "with-icon" and the `iconName` prop to set the icon.

<Canvas>
  <Story
    name="With Icon"
    args={{
      variant: 'with-icon',
      iconName: 'plus',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Title

Use the `variant` prop as "with-title" and the `title` prop to set the title.

<Canvas>
  <Story
    name="With Title"
    args={{
      variant: 'with-title',
      title: 'Title',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Button

Use the `variant` prop as "with-button" and the `buttonText` prop to set the button text.

<Canvas>
  <Story
    name="With Button"
    args={{
      variant: 'with-button',
      buttonText: 'Button',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## With Title and Button

Use the `variant` prop as "with-title-and-button" and the `title` and `buttonText` props to set the title and button text.

<Canvas>
  <Story
    name="With Title and Button"
    args={{
      variant: 'with-title-and-button',
      title: 'Title',
      buttonText: 'Button',
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>

## Positions

Use the `position` prop to set the position of the divider. The default position is `center`.

<Canvas>
  <Story name="Positions">{Positions.bind({})}</Story>
</Canvas>

## Referance

<ArgsTable of={Divider} />
