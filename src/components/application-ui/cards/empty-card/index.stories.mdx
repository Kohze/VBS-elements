import {
  Meta,
  Canvas,
  ArgsTable,
  Story,
  Preview,
  Source,
} from '@storybook/addon-docs'
import VBSEmptyCard from '@/components/application-ui/cards/empty-card'

<Meta title="Application UI/Cards/Empty Card" component={VBSEmptyCard} />

export const Template = (args) => <VBSEmptyCard {...args} />

export const WithChildren = (args) => (
  <VBSEmptyCard {...args}>
    <div>Custom content</div>
  </VBSEmptyCard>
)

export const WithCustomClasses = (args) => (
  <VBSEmptyCard className="bg-slate-500 text-slate-50 px-8 py-16" {...args}>
    <div>Custom content</div>
  </VBSEmptyCard>
)

# Empty Card

Empty card is a wrapper component that can be used to display a card with a title, description and so on.

## Default

<Canvas>
  <Story name="Empty Card">{Template.bind({})}</Story>
</Canvas>

## With Children

<Canvas>
  <Story name="With Children">{WithChildren.bind({})}</Story>
</Canvas>

## With Custom Classes

Use the `className` prop to add custom or tailwind classes to the card.

<Canvas>
  <Story name="With Custom Classes">{WithCustomClasses.bind({})}</Story>
</Canvas>

## Reference

<ArgsTable of={VBSEmptyCard} />
